# رحلة بناء نظام Clinic_API - الدليل التعليمي الشامل 🎓

مرحباً بك أيها المطور الطموح! سنأخذك اليوم في رحلة تعليمية شيقة لنبني معاً نظام `Clinic_API` خطوة بخطوة. سنتعلم كيف ننشئ المشروع، نجعله آمناً، نخصصه ليعكس هويتنا، وكيف نرتب أفكارنا وكودنا ليصبح نظاماً احترافياً.

---

## الفصل الأول: البداية (Inception) 🌱

كل نظام عظيم يبدأ بقرار. قرارنا اليوم هو بناء واجهة برمجية (API) حديثة وخفيفة (Minimal API) باستخدام .NET.

### 1. إنشاء المشروع
فتحنا سطر الأوامر (Terminal) في المجلد الذي نريد العمل فيه، وكتبنا الأمر التالي ليقوم .NET بإنشاء الهيكل الأساسي لنا:

```bash
dotnet new webapi -n Clinic_API
```

*   **dotnet new**: أمر الإنشاء.
*   **webapi**: نوع المشروع (مشروع API جاهز).
*   **-n Clinic_API**: اسم مشروعنا الجديد.

بهذا الأمر، حصلنا على مجلد جديد يحتوي على ملف `Program.cs` وهو القلب النابض للنظام.

---

## الفصل الثاني: الأمان أولاً (Security First) 🔒

في عالم الويب، الثقة هي كل شيء. لذلك، كان لزاماً علينا اعتماد بروتوكول HTTPS المشفر منذ اللحظة الأولى.

### 1. شهادة الثقة (Trust Certificate)
لجعل المتصفح يثق في "السيرفر المحلي" الخاص بنا أثناء التطوير، قمنا بإصدار شهادة تطوير موثوقة:

```bash
dotnet dev-certs https --trust
```

### 2. إعدادات التشغيل (Launch Settings)
تأكدنا من أن النظام يعمل افتراضياً على HTTPS من خلال ملف `Properties/launchSettings.json`. قمنا بترتيب الأولويات ليكون ملف تعريف `https` هو الأول.

---

## الفصل الثالث: التشغيل والرؤية (First Run) 🚀

حان وقت رؤية ثمرة عملنا!

### تشغيل البرنامج
استخدمنا الأمر السحري الذي يراقب ملفاتنا ويعيد تشغيل النظام كلما أجرينا تعديلاً:

```bash
dotnet watch run
```

بمجرد التشغيل، انطلق المتصفح تلقائياً إلى صفحة **Swagger UI**.
ما هو Swagger؟ هو "كتالوج" تفاعلي يوثق لنا كل الخدمات (Endpoints) التي يقدمها نظامنا، ويسمح لنا بتجربتها بضغطة زر.

---

## الفصل الرابع: لمستنا الخاصة (Branding) 🎨

الوضع الافتراضي لـ Swagger جيد، لكننا نريد أن يشعر المستخدم أنه في نظام "Barefoot Clinic".

### 1. الشعار (Logo)
لم نرضَ بالشعار الافتراضي.
*   أنشأنا مجلداً للملفات الثابتة: `wwwroot/images`.
*   وضعنا فيه شعارنا الجميل: `logo.png`.

### 2. التجميل (CSS)
أردنا تغيير الألوان وإخفاء الشريط العلوي الافتراضي.
*   أنشأنا ملف: `wwwroot/css/swagger-custom.css`.
*   كتبنا فيه كود CSS ليجعل الشعار بحجم 50px متناسق، ويغير العناوين.

### 3. تفعيل التخصيص
لكي يرى النظام هذه الملفات، قمنا بخطوتين في الكود:
1.  **السماح بالملفات الثابتة**: أضفنا `app.UseStaticFiles()` في `Program.cs`.
2.  **حقن التصميم**: أخبرنا Swagger أن يستخدم ملف الـ CSS الخاص بنا، وغيرنا عنوان الصفحة إلى "Barefoot Swagger Documentation".

النتيجة؟ واجهة احترافية تحمل هويتنا البصرية! ✨

---

## الفصل الخامس: من الفوضى إلى النظام (Evolution & Structure) 🏗️

في البداية، كان كل شيء في ملف واحد (`Program.cs`). مع نمو النظام، أصبح هذا الملف مزدحماً جداً. هنا قررنا أن نكون "مهندسين" وننظم بيتنا.

إليك هيكل النظام كما هو الآن، ولماذا فعلنا ذلك:

### 1. المجلد `Models` (نماذج البيانات)
بدلاً من كتابة تعريفات البيانات (مثل المستخدم `TblUser`، الموظف `StrEmployee`) في كل مكان، جمعناها هنا. هذا هو "شكل" البيانات التي يتعامل معها النظام.

### 2. المجلد `Data` (قاعدة البيانات)
هنا يعيش `ClinicDbContext`. هو المسئول عن التحدث مع قاعدة البيانات. بدل أن نكتب SQL يدوياً، نستخدم هذا الكلاس ليترجم كود C# إلى أوامر قاعدة بيانات (EF Core).
*   **تحدي وحل**: واجهتنا مشكلة في مفتاح الجدول للمستخدم (User)، فقمنا بتعديله هنا في `OnModelCreating` ليعمل بسلاسة.

### 3. المجلد `Extensions` (الخدمات والإضافات)
هذا هو "صندوق العدة" الخاص بنا.
*   **`ServiceCollectionExtensions.cs`**: هنا نجهز الخدمات قبل تشغيل التطبيق (مثل إعداد قاعدة البيانات، إعداد التوثيق JWT).
*   **`EndpointsExtensions.cs`**: هذا أهم ملف تنظيم! بدلاً من زحام `Program.cs`، نقلنا تعريفات الروابط (Endpoints) هنا.
    *   `MapUserEndpoints`: تحتوي على كل ما يخص المستخدمين (إضافة، تعديل، حذف).
    *   هنا وضعنا أيضاً منطق **التحقق (Validation)**: "هل اسم المستخدم مكرر؟" لضمان جودة البيانات.

### 4. المجلد `Middleware` (الحراس)
*   **`GlobalExceptionMiddleware.cs`**: هو الحارس الذي يقف على الباب. إذا حدث أي خطأ في النظام (Crash)، يمسكه هذا الحارس ويعيد رسالة مهذبة ومفهومة للمستخدم بدلاً من توقف النظام المفاجئ.

---

## الخاتمة

وهكذا يا صديقي، تحول مشروعنا من مجرد أمر `dotnet new` بسيط، إلى نظام متكامل، آمن، جميل المظهر، ومرتب الكود بعناية.

هذا النظام الآن جاهز للتوسع وإضافة المزيد من الميزات، لأن أساسه متين ومنظم. 🏗️🚀
